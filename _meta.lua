local _ = require("gettext")
return {
    name = "readingos",
    fullname = _("ReadingOS Dashboard"),
    description = _([[A reading-first home screen: the current book, what is due today, what the house is out of, what to watch next, a card to review — and The Dig, which grows out of all of it.]]),
}
